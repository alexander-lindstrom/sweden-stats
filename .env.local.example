# Copy this to .env.local and fill in your values.
# .env.local is gitignored — never commit it.

# Point local dev at the hosted GeoServer instead of running one locally.
# Leave unset (or remove) to fall back to http://localhost:8080.
GEOSERVER_URL=http://your-hetzner-ip
